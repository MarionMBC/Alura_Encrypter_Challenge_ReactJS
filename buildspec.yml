version: 0.2

phases:
  pre_build: 
    commands:
      - npm install
  build:
    commands:
      - npm run build 
  
  post_build:
    commands:
      - zip -r syncpro.zip build/*

artifacts:
  files:
    - syncpro.zip